/* 
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */


document.getElementById("span_cookie_nom_2").innerHTML = "Cookie del cliente 'con nombre búsqueda': " + document.cookie;

document.getElementById("span_cookie_nom").innerHTML = "Cookie del cliente 'sin nombre búsqueda': " + getCookie("nombre_busqueda"); /* sin nombre_busqueda */

/* copiado de internet */
function getCookie(cname) {
  var name = cname + "=";
  var decodedCookie = decodeURIComponent(document.cookie);
  var ca = decodedCookie.split(';');
  for(var i = 0; i <ca.length; i++) {
    var c = ca[i];
    while (c.charAt(0) == ' ') {
      c = c.substring(1);
    }
    if (c.indexOf(name) == 0) {
      return c.substring(name.length, c.length);
    }
  }
  return "";
}